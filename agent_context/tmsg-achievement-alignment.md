# 天命三国成就对齐记录

## 已对齐分类

当前本地成就分类已先对齐为以下顺序：

1. 道具
2. 成长
3. 副本
4. 杀怪
5. 社交
6. P K
7. 赛事
8. 装备
9. 财富
10. 副将
11. 活动
12. 任务
13. 其他
14. 长安
15. 大漠
16. 沧海

## 第一类实现范围

这一轮先完整落地了“道具”分类。

- 消耗类：`34` 组，每组按 `10 / 100 / 1000` 三档成就生成
- 宝匣类：`7` 组，每组按 `20 / 100` 两档成就生成

总计：`116` 条“道具”成就

## 本地记录位置

“道具”分类的每个成就名称、要求、成就点、永久属性奖励，已经逐条记录在：

- `services/achievement_catalog.py`

说明：

- `ITEM_SERIES` 逐条记录了每个消耗类成就母项
- `CHEST_SERIES` 逐条记录了每个宝匣类成就母项
- `ITEM_TIER_DEFINITIONS` 记录了 `开始磕 / 药就是饭 / 吃一辈子`
- `CHEST_TIER_DEFINITIONS` 记录了 `开始开 / 开箱高手`
- 运行时会展开成 116 条实际成就定义
